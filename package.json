{
  "scripts": {
    "build": "npm install && npm install forever -g && forever start main2.js && forever list"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "forever": "^4.0.3",
    "node-fetch": "^2.6.7",
    "node-telegram-bot-api": "^0.60.0"
  }
}
